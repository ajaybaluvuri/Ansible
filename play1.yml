---
- name: Writing a test playbook
  hosts: all
  remote_user: ubuntu
  gather_facts: yes
  become: yes
  tasks:
    - name: Task1
      shell: sudo apt update
    - name: Task2
      shell: df -h
    - name: Task3
      shell: pwd
